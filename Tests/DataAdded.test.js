const { getCurrenciesURL, getRatesURL } = require("../Utils/Constants");
const DataAdder = require("../Utils/DataAdder");

test("Gets content from certain request", () => {
  expect((1 + 2).toBe(3));
});
